# UI Roadmap

- Rich selection UX: breadcrumbs by path, keyboard navigation (↑ to parent, ←/→ siblings).
- Rule search/filter and quick keyboard shortcuts.
- Pretty math typesetting pass (later) after engine stabilizes.
- Session export/import (JSON of traces & snapshots).
