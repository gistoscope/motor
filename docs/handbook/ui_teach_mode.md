# Teach Mode UI

Teach mode exposes TSA atoms through a click-driven UI. The front-end routes clicks to `@motor/tsa-policy/engine`, which applies the active learner profile. Rendering uses `render/Pretty.tsx` to show fraction bars without simplifying.
