# Developer Runbook

- Install dependencies with `pnpm install`.
- Run `pnpm verify` to build all workspace packages and execute tests.
- Use `pnpm --filter @motor/web dev` to iterate on the teach-mode UI locally.
