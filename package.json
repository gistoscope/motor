{
  "name": "motor-unified-stage1",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "verify": "tsc -p tsconfig.base.json --noEmit",
    "test": "pnpm -r test",
    "forbidden-tokens": "node scripts/forbidden-tokens.cjs"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "happy-dom": "^19.0.2",
    "husky": "^8.0.3",
    "lint-staged": "^16.2.3",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
