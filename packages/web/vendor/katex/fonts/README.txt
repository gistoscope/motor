This directory would normally contain KaTeX font assets when bundling the full
KaTeX distribution. The demo build only relies on system fonts, so these files
are intentionally omitted in this lightweight vendor copy.
